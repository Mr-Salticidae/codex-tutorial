---
tags: [类型/交接, 主题/教程制作, 状态/待执行]
---

# 交接 · 朱元璋K线人生待补配图 · 致 Claude 桌面 app

> 致 Claude 桌面 app：这是 Claude Code 会话（VS Code 内）移交的截图归档任务。
> 创建时间：2026-07-22
> 项目阶段：「朱元璋K线人生」大纲 v0.2 + 附件版 + 8 张配图已完成并推送（`64f171c`），剩 4 张对话/发布截图待补。
> 移交原因：这 4 张必须取自**真实 Codex 会话**，需要屏幕操作（切窗口、滚动到指定对话位置）。本会话没有 computer use 工具，无鼠标键盘控制权；桌面 app 有屏幕操作能力。
> 文档版本：v1，来源为跳蛛先生 2026-07-22 口头指令「先存档，然后写交接文档，让 Claude 桌面 app 接手执行截图归档任务」。

---

## 一、任务

在 `e:\codex-tutorial\docs\images\朱元璋K线人生\` 下补齐 4 张 PNG，文件名必须与下表**逐字一致**（附件版里是按此路径写死的 🔴 占位，名字错一个字就接不上）。

## 二、待补清单

| 文件名（必须逐字一致） | 该图要拍什么 | 附件版所在位置 |
|---|---|---|
| `图03_定口径对话.png` | 把 K 线五个价格翻译成人生含义的那段对话。正文紧接着的提示词是「先别写代码，我们先定口径：开盘=阶段起点处境……你复述一遍确认我们理解一致」，截图应能看到这条指令与 Codex 的复述确认 | 第 29 行 |
| `图05_结构化节点数组对话.png` | Codex 吐出结构化历史节点数组的那段。最好能看到数组里的字段（`year`/`age`/`title`/`open`/`high`/`low`/`close`/`volume`/`note`），体现「27 个年龄切片」 | 第 41 行 |
| `图09_节奏调整反馈对话.png` | 提节奏需求那段。原话是「现在这个播放像视频一样一口气放完，观众太被动……总目标是增加画面张力」，截图应能看到这条需求与 Codex 的回应 | 第 77 行 |
| `图11_发布Toy.png` | 发布成 B站 Toy 的对话，或 B站发布台/线上 Toy 页面。已上线信息：项目 id `13050178493440`，slug `zhu-yuanzhang-kline`，地址 `https://www.bilibili.com/toy/zhu-yuanzhang-kline/index.html` | 第 89 行 |

## 三、硬约束

1. **不得伪造。** 这四张必须是真实 Codex 会话/真实发布页面的截图。不要用生成图、示意图或重演画面冒充对话截图。若某段对话已不可回溯，**宁可留空并回报**，不要造一张。
2. **隐私裁切。** 截屏时该机屏幕上同时开着微信群聊等私人窗口（已确认联系人名与消息可读）。这些图要进公开教程，**只裁 Codex 窗口区域**，不得带入：微信/QQ 等聊天窗口、联系人名、无关标签页、桌面通知。
3. **格式**：PNG。同目录既有图为 1440×900（应用截图）与 2400×1350（示意卡片），对话图不强制尺寸，但要求**正文文字清晰可读**，按窗口区域裁切、不留大片空白桌面。

## 四、归档动作（截完后）

1. 存入 `e:\codex-tutorial\docs\images\朱元璋K线人生\`，文件名照第二节。
2. 校验 12 处占位是否全部落地：

```bash
cd e:/codex-tutorial
grep -n '此处插入图' docs/朱元璋K线人生_附件版.md \
  | sed 's/.*源文件：//; s/】.*//' \
  | while IFS= read -r p; do [ -f "$p" ] && echo "✅ $p" || echo "❌ 缺 $p"; done
```

   期望：12 行全 ✅（当前为 8 ✅ / 4 ❌）。

3. 更新 `README.md`：把「4 张对话/发布图待作者提供真实截图」那条待办从 `- [ ]` 改为 `- [x]` 并注明补齐日期；若只补了部分，只改说明不要打勾。
4. commit + push（本仓 commit 信息用中文，结尾带 `Co-Authored-By` 行，参照 `64f171c`）。

## 五、验证状态（本会话侧，移交前已完成）

- 附件版格式白名单自查：**通过**（0 引用块 / 0 表格 / 0 多级小标题 / 0 分隔线 / 0 代码块，1 个一级标题）
- 8 张已产出配图：**已入库并推送**（`64f171c`）
- 两仓工作区：**干净、已推送**（codex-tutorial `64f171c`；above-the-web `c7dc71d`）
- 4 张对话图：**未执行**——本会话无屏幕操作能力，即本次移交事由
- 30s 演示视频：**未执行**，不在本次移交范围（见第六节）

## 六、需要跳蛛先生确认的事项

- [ ] 图11 用「发布对话」还是「线上 Toy 页面」？两者都符合正文语境，线上页面更稳（不依赖回溯对话）。
- [ ] 若图03/05/09 对应的原始对话已不可回溯，是否接受**减图**方案：把该处 🔴 占位从附件版删除、正文微调衔接，而不是补一张不真实的图。
- [ ] `朱元璋K线人生_30s演示.mp4` 仍缺（附件版首屏留了 🔴 附件占位），是否一并交给桌面 app 录制。

反对路径：

- 若不接受「减图」，可保留 🔴 占位不动——附件版是 md 源稿，占位不影响仓库状态，只在导入钉钉时才需要落图；届时再补不迟。
- 若不接受由桌面 app 代截，可由跳蛛先生自行截图后把文件路径给任一会话，归档动作（重命名、入库、README、提交）本会话即可完成，无需屏幕权限。

---

## 作者 / 接收 / 下游

**作者**：Claude Code（VS Code 会话，e:\codex-tutorial）
**接收方**：Claude 桌面 app
**决策人**：跳蛛先生
**下游**：图齐后并入钉钉母文档 → verbatim 回填终版（并入前按范文参考骨架律把各板块开头连读一遍）
